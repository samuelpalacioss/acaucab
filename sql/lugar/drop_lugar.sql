/**
 * Script para eliminar la tabla lugar
 * Elimina la tabla que almacena los lugares (Estados, Municipios, Parroquias)
 * Usa CASCADE para eliminar automáticamente las dependencias
 */
DROP TABLE lugar CASCADE;
